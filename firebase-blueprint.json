{
  "entities": {
    "GameRequest": {
      "title": "Game Request",
      "description": "A request for a new game to be added to the library.",
      "type": "object",
      "properties": {
        "gameName": {
          "type": "string",
          "description": "The name of the game being requested."
        },
        "gameUrl": {
          "type": "string",
          "description": "The URL of the game (if known)."
        },
        "requesterEmail": {
          "type": "string",
          "format": "email",
          "description": "The email of the person making the request."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the request was made."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "approved", "rejected"],
          "description": "The current status of the request."
        }
      },
      "required": ["gameName", "createdAt", "status"]
    }
  },
  "firestore": {
    "requests": {
      "schema": "GameRequest",
      "description": "Collection of game requests from users."
    }
  }
}
